#include "main.h"
/**
 *add - function that adds two integers and returns the result.
 *@a : first integer
 *@b : second integer
 *Return : result
 */

int add(int a, int b)
{
	int a = 1;
	int b = 1;
	int result;

	result = add(a, b);
	return (result);
}

