#include <stdio.h>

/**
 *_strlen - the function that returns the length of a string.
 *@s:string to be return
 *Return:length
 */
int _strlen(char *s)
{
	int length = 0;

	while (*str++)
		length++;
	return (length);
}
